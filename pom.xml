<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.joxette</groupId>
    <artifactId>joxette</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>joxette</name>
    <description>Kafka Topic Cassette Recorder backed by DuckLake</description>

    <properties>
        <!-- jox structured is compiled as Java 25 preview — this project must be built
             and tested with JDK 25. Use: sdk use java 25.0.2-tem (see .sdkmanrc)         -->
        <java.version>25</java.version>
        <compiler.source>25</compiler.source>
        <compiler.release>25</compiler.release>
        <jox.channels.version>1.1.2</jox.channels.version>
        <jox.flows.version>0.5.3</jox.flows.version>
        <duckdb.version>1.5.1.0</duckdb.version>
        <jooq.version>3.21.1</jooq.version>
        <springdoc-openapi-starter-webmvc-ui.version>3.0.2</springdoc-openapi-starter-webmvc-ui.version>
        <aws.sdk.version>2.42.32</aws.sdk.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>4.0.5</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
<dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Spring Boot Web (REST API) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Actuator (health, metrics) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Micrometer Prometheus registry -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Configuration metadata processor -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Jox channels: virtual-thread channels -->
        <dependency>
            <groupId>com.softwaremill.jox</groupId>
            <artifactId>channels</artifactId>
            <version>${jox.channels.version}</version>
        </dependency>

        <!-- Jox flows: reactive-style pipeline operators -->
        <dependency>
            <groupId>com.softwaremill.jox</groupId>
            <artifactId>flows</artifactId>
            <version>${jox.flows.version}</version>
        </dependency>

        <!-- Jox structured concurrency -->
        <dependency>
            <groupId>com.softwaremill.jox</groupId>
            <artifactId>structured</artifactId>
            <version>${jox.flows.version}</version>
        </dependency>

        <!--
            com.softwaremill.jox:kafka v0.5.3 exists in source at
            https://github.com/softwaremill/jox/tree/master/kafka
            but has NOT been published to Maven Central.

            com.joxette.kafka provides an equivalent API surface (KafkaSource,
            KafkaSink, ConsumerSettings, ProducerSettings) in the interim, mirroring
            the jox:kafka design so the migration is mechanical when the module ships:
              1. Add the dependency below.
              2. Delete src/main/java/com/joxette/kafka/.
              3. Update the two import sites (KafkaConfig, KafkaProducerService).

            <dependency>
                <groupId>com.softwaremill.jox</groupId>
                <artifactId>kafka</artifactId>
                <version>${jox.flows.version}</version>
            </dependency>
        -->

        <!-- Apache Kafka clients (direct dependency until jox:kafka is published) -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
        </dependency>

        <!-- DuckDB JDBC driver -->
        <dependency>
            <groupId>org.duckdb</groupId>
            <artifactId>duckdb_jdbc</artifactId>
            <version>${duckdb.version}</version>
        </dependency>

        <!-- jOOQ: type-safe SQL DSL -->
        <dependency>
            <groupId>org.jooq</groupId>
            <artifactId>jooq</artifactId>
            <version>${jooq.version}</version>
        </dependency>

        <!-- SpringDoc OpenAPI (Swagger UI + api-docs) -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc-openapi-starter-webmvc-ui.version}</version>
        </dependency>

        <!-- AWS SDK v2 S3 client (snapshot export to object store) -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
        </dependency>

        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
        </dependency>

        <!-- Jackson for JSON handling in REST API -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- JSONPath for entity ID extraction from message values -->
        <dependency>
            <groupId>com.jayway.jsonpath</groupId>
            <artifactId>json-path</artifactId>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers core -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers JUnit 5 integration -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers Kafka (Redpanda compatible) -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-kafka</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers MinIO — for DuckLake S3 integration tests -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-minio</artifactId>
            <scope>test</scope>
        </dependency>

<!--        &lt;!&ndash; AWS SDK v2 S3 client — used in tests to create MinIO buckets &ndash;&gt;-->
<!--        <dependency>-->
<!--            <groupId>software.amazon.awssdk</groupId>-->
<!--            <artifactId>s3</artifactId>-->
<!--            <scope>test</scope>-->
<!--            <exclusions>-->
<!--                &lt;!&ndash; Prefer the lightweight URL-connection HTTP client over Netty/Apache &ndash;&gt;-->
<!--                <exclusion>-->
<!--                    <groupId>software.amazon.awssdk</groupId>-->
<!--                    <artifactId>netty-nio-client</artifactId>-->
<!--                </exclusion>-->
<!--                <exclusion>-->
<!--                    <groupId>software.amazon.awssdk</groupId>-->
<!--                    <artifactId>apache-client</artifactId>-->
<!--                </exclusion>-->
<!--            </exclusions>-->
<!--        </dependency>-->
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${compiler.release}</release>
                    <source>${compiler.source}</source>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                        <arg>-parameters</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <jvm>${java.home}/bin/java</jvm>
                    <argLine>--enable-preview</argLine>
                    <systemPropertyVariables>
                        <!-- Skip Spring's classpath scanner failing on preview anonymous
                             classes (e.g. ParameterizedTypeReference<>(){}) -->
                        <spring.classformat.ignore>true</spring.classformat.ignore>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <jvmArguments>--enable-preview</jvmArguments>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jooq</groupId>
                <artifactId>jooq-codegen-maven</artifactId>
                <version>${jooq.version}</version>
                <executions>
                    <execution>
                        <id>jooq-codegen</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <!-- DDLDatabase interprets DDL via jOOQ's parser; no JDBC driver needed -->
                    <dependency>
                        <groupId>org.jooq</groupId>
                        <artifactId>jooq-meta-extensions</artifactId>
                        <version>${jooq.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <generator>
                        <database>
                            <name>org.jooq.meta.extensions.ddl.DDLDatabase</name>
                            <!-- DDLDatabase places unqualified tables in PUBLIC -->
                            <inputSchema>PUBLIC</inputSchema>
                            <properties>
                                <property>
                                    <key>scripts</key>
                                    <value>src/main/resources/db/jooq-codegen-schema.sql</value>
                                </property>
                                <!-- Preserve lower-case table/column names from the DDL -->
                                <property>
                                    <key>defaultNameCase</key>
                                    <value>as_is</value>
                                </property>
                                <property>
                                    <key>sort</key>
                                    <value>semantic</value>
                                </property>
                            </properties>
                        </database>
                        <target>
                            <packageName>com.joxette.db.jooq</packageName>
                            <directory>target/generated-sources/jooq</directory>
                        </target>
                    </generator>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
